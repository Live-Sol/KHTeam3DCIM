spring.application.name=KHTeam3DCIM

# --- Oracle DB Connection ---
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:XE
spring.datasource.username=scott
spring.datasource.password=tiger
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# --- Hibernate / JPA ---
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.show-sql=true
# 테이블 자동 생성/업데이트
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# --- Connection Pool (HikariCP) ---
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.pool-name=HikariCP

#
#
###############################################
## Spring Boot 기본 서버 설정
###############################################
#
## 서버 포트 지정 (기본 8080)
#server.port=8080
#
## context-path 지정 (웹 프로젝트 루트 경로)
#server.servlet.context-path=/dcim
#
###############################################
## Oracle DB 연결 설정
###############################################
#
## JDBC URL: Oracle DB 접속 주소
#spring.datasource.url=jdbc:oracle:thin:@localhost:1521:xe
#
## DB 사용자 이름
#spring.datasource.username=scott
#
## DB 비밀번호
#spring.datasource.password=tiger
#
## Oracle JDBC 드라이버 클래스
#spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
#
## SQL 로그 출력 (개발 편의)
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#
###############################################
## JPA / Hibernate 설정
###############################################
#
## ddl-auto 옵션
## none : 아무것도 안함
## update : Entity 기준으로 DB 스키마 자동 변경
## create : 시작할 때 테이블 새로 생성
## create-drop : 시작 시 생성, 종료 시 삭제
#spring.jpa.hibernate.ddl-auto=update
#
## Oracle 사용 시 dialect 지정 (선택)
#spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect
#
##############################################
# Thymeleaf 설정
##############################################

# 템플릿 캐시 비활성화 (개발 편의, 변경 시 즉시 반영)
spring.thymeleaf.cache=false

# 템플릿 경로 (기본값)
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

##############################################
# DevTools 설정
##############################################

# DevTools 자동 재시작 활성화 (기본 true)
spring.devtools.restart.enabled=true

# LiveReload 브라우저 자동 새로고침 활성화 (기본 true)
spring.devtools.livereload.enabled=true
#
###############################################
## 로깅 (선택)
###############################################
#
## SQL 로그 레벨 DEBUG
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
